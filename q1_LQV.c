#include <stdio.h>
//supose an array of size >= 6 is given. it value must be initialized by integers of your choice. 
//arrage this array and the output array so that perfect square numbers are arranged first and then non perfect square numbers
//Note: perfect square numbers are natural numbers whose square root is also natural
//write the function  to check a number is a perfect square (do not use math.h library function)
// write the function to re-arrange perfect square numbers position in the array
//write the function to display an input array
int print_array(int arr[], int size)
{
    for(int i = 0; i < size; i++)
    {
        
    }
}
//write the main function to test all the above functions.
//Calculate the complexity of algo

//O(sqrt(n))
